1) git checkout master--->switch to branch master
2)git branch---------->check the branch
3)git checkout -b developer -------->create the new branch of name developer
4)git log------->all activity happen in that repocitory
5)git merge developer ----->merg all changes happen in the developer branch to the new branch
            new branch(master) (you need to switch first to the new branch i.e(git checkout master)) 
6)git add Files/project.c   --->add only File/project.c file (tha is change now)
7)git commit -m "removing space"--->reson to change
8)git status--->show the status of repocitory
9)git push origin staging-----> push the staging branch code to the origin branch




